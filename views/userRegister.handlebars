{{!

If the user is NOT logged in, and 'GET /register' endpoint is called, you will render this sign-up form view that displays two input (one for the username and one for password):
-The form will be used to submit the POST request to the '/register' route on the server and must have an 'id' of 'registration-form'
-The input for the username must have a 'name' and 'id' of 'usernameInput'
-The input for the password must have a 'name' and 'id' of 'passwordInput' and should be an input type of password.

Do not forget to use labels for your inputs! For the labels, you will use the method of using the 'for' attribute. 
You should have a label for both the username and password inputs referencing their id's in the for attribute of the label

You will also have a link on this page that links to '/' and has the text "Already have an account? Click here to log-in"

}}

<main>
  <h1>User Register Page</h1>
  <p>Register on our website</p>

  <span style="color: red;"> {{error_msg}} </span>

  <div class="inputFieldsId" id="inputFieldsId">

    <form
      id="registartion-form"
      autocomplete="off"
      method="post"
      action="/register"
    >
<table id="tableInput"> <tr> <td>

      {{! profile picture }}

      {{!-- <input
        id="image_input"
        type="file"
        name="profile_photo"
        placeholder="Photo"
        accept="image/png, image/jpg,image/jpeg" onclick="clickImage()"
      />
      <div id="display_image">
        <image id="profileImage" src="http://lorempixel.com/100/100" />
      </div>  --}}

      
      <br />

      {{!-- first name --}}
        <label for="firstname">First Name* :</label>
        <input  type="text" id="firstnameInput" name="firstnameInput" autocomplete="name" enterkeyhint="next">
{{!-- last name --}}
        <label for="lastname">Last Name* :</label>
        <input  type="text" id="lastnameInput" name="lastnameInput" autocomplete="name" enterkeyhint="next">

<br /><br />
      {{! enter username }}
      <label for="username">Enter Username* :</label>
      <input
        id="usernameInput"
        type="text"
        name="usernameInput"
        placeholder=""
      />
      <br /><br />

      {{! enter mail id }}
      <label for="email">Enter your email* :</label>
      <input type="email" id="emailInput" name="emailInput"  placeholder="" />

      <br /><br />

      {{! enter dob }}
      <label for="birthday">Birthday:</label>
      <input type="date" id="birthdayInput" name="birthdayInput" />
      <br /><br />

      {{! choose gender }}
       <legend>Choose your gender:
      <select name="genderInput" id="genderInput">
      <option value="Male">Male</option>
      <option value="Female">Female</option> 

    </select>
    </legend>

      {{!-- <fieldset data-role="controlgroup">
        <legend>Choose your gender:</legend>
        <label for="male">Male</label>
        <input
          type="radio"
          name="maleInput"
          id="maleInput"
          value="male" checked
        />
        <label for="female">Female</label>
        <input
          type="radio"
          name="femaleInput"
          id="femaleInput"
          value="female"
        />
      </fieldset> --}}

      <br />

    {{!-- address fields --}}
     {{> page/address}}
      {{! contact }}
      <label for="phone">Enter a phone number:</label>
      <input
        type="text"
        id="phoneInput"
        name="phoneInput"
        placeholder="123-45-678"
        
        
      /><br /><br />
      {{! password }}
      <label for="password">Create Password* :</label>
      <input
        id="passwordInput"
        type="password"
        name="passwordInput"
        placeholder="Password"
      />

     

         <br /><br />

    <select name="personInput" id="personInput">
      <option value="Student">Student</option>
      <option value="Instructor">Instructor</option>

    </select>
    <br /><br />


      <input id="submit-button" type="submit" value="Submit" />
</td> </tr> 

      </table> 
    </form>

  </div>
  {{#if error}}
    <h6>{{error}}</h6>
  {{/if}}


  

</main>


<a href="/" id="hrefid"> <br />Already have an account? Click here to log-in  <br /><br /></a>

